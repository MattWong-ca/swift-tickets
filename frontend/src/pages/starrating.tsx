import styles from '../styles/index.module.css';
import { useEffect, useState } from 'react';

export default function StarRating() {

    // To do: implement a star rating that changes color when hovered, and then console logs the # corresponding to the star if they click

    return (
        <div>
           
        </div>
    );
}